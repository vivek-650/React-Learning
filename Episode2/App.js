import React from "react";
import { ReactDOM } from "react-dom/client";

const parent = React.createElement("h1", {}, " React is on 🚀")

const root = ReactDom.createRoot(document.getElementById(root));
root.render(parent);